<Window x:Class="TRPServerPanel.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About TRP" Height="380" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ShowInTaskbar="False">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <Border Margin="15" Background="{StaticResource BackgroundBrush}" 
                BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="1" CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect Color="#FF0000" BlurRadius="25" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Background="#0A0A0A" BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="0,0,0,1" CornerRadius="12,12,0,0" MouseLeftButtonDown="Header_Border_MouseLeftButtonDown">
                    <Grid Margin="15,0" WindowChrome.IsHitTestVisibleInChrome="True">
                        <TextBlock Text="ИНФОРМАЦИЯ" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button HorizontalAlignment="Right" VerticalAlignment="Center" 
                                Style="{StaticResource WindowControlCloseButtonStyle}" 
                                Width="30" Height="24" Click="Close_Click">
                            <Viewbox Width="8" Height="8">
                                <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                                      Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform"/>
                            </Viewbox>
                        </Button>
                    </Grid>
                </Border>

                <!-- Content -->
                <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path Data="M12,2L4.5,20.29L5.21,21L12,18L18.79,21L19.5,20.29L12,2Z" 
                          Fill="{StaticResource PrimaryBrush}" Width="48" Height="48" Stretch="Uniform" Margin="0,0,0,15">
                        <Path.Effect><DropShadowEffect Color="#FF0000" BlurRadius="15" ShadowDepth="0" Opacity="1"/></Path.Effect>
                    </Path>
                    <TextBlock Text="TRP SERVER PANEL" Foreground="White" FontSize="18" FontWeight="Black" HorizontalAlignment="Center"/>
                    <TextBlock Text="VERSION 5.0.0-HYBRID" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" HorizontalAlignment="Center" Margin="0,5,0,20"/>
                    
                    <TextBlock Text="Developed by TEAM RUST PLUGINS" Foreground="{StaticResource TextMutedBrush}" FontSize="10" HorizontalAlignment="Center"/>
                    <TextBlock Text="© 2026. All rights reserved." Foreground="{StaticResource TextMutedBrush}" FontSize="9" HorizontalAlignment="Center" Margin="0,2"/>
                </StackPanel>

                <!-- Footer / Links -->
                <Border Grid.Row="2" Background="#0C0C0C" BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="0,1,0,0" CornerRadius="0,0,12,12">
                    <UniformGrid Columns="2" Margin="10,0">
                        <Button Style="{StaticResource PremiumActionButtonStyle}" Content="DISCORD" Margin="5"/>
                        <Button Style="{StaticResource PremiumActionButtonStyle}" Content="WEBSITE" Margin="5"/>
                    </UniformGrid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
