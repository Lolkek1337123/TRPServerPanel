<Window x:Class="TRPServerPanel.Views.InstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rust Server Installer" Height="650" Width="750"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="InstallStepTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Black"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style x:Key="FormFieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#808080"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
        </Style>
        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="BorderBrush" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#FF4444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Margin="15" Background="{StaticResource BackgroundBrush}" 
                BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="1" CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect Color="#FF0000" BlurRadius="25" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="70"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Background="#0A0A0A" BorderThickness="0" CornerRadius="12,12,0,0" MouseLeftButtonDown="Header_Border_MouseLeftButtonDown">
                    <Grid Margin="15,0" WindowChrome.IsHitTestVisibleInChrome="True">
                        <TextBlock Text="{DynamicResource Str_InstallConfig}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button HorizontalAlignment="Right" VerticalAlignment="Center" 
                                Style="{StaticResource WindowControlCloseButtonStyle}" Click="Close_Click" ToolTip="{DynamicResource Str_Cancel}">
                            <Viewbox Width="8" Height="8">
                                <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                                      Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform"/>
                            </Viewbox>
                        </Button>
                    </Grid>
                </Border>

                <!-- Content Area -->
                <Grid Grid.Row="1" Margin="35,25">
                    
                    <!-- STEP 1: CONFIGURATION FORM -->
                    <ScrollViewer x:Name="Step1" Visibility="Visible" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Str_ServerSetup}" Style="{StaticResource InstallStepTitleStyle}"/>
                            <TextBlock Text="{DynamicResource Str_FillData}" Foreground="#505050" FontSize="11" Margin="0,0,0,25"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Column 1: General & Connection -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{DynamicResource Str_BasicData}" Foreground="{StaticResource PrimaryBrush}" FontSize="10" FontWeight="Bold"/>
                                    
                                    <TextBlock Text="{DynamicResource Str_ServerName}" Style="{StaticResource FormFieldLabelStyle}"/>
                                    <TextBox Style="{StaticResource FormTextBoxStyle}" Text="My New Rust Server"/>

                                    <TextBlock Text="{DynamicResource Str_IPAddress}" Style="{StaticResource FormFieldLabelStyle}"/>
                                    <TextBox Style="{StaticResource FormTextBoxStyle}" Text="0.0.0.0"/>

                                    <TextBlock Text="{DynamicResource Str_RconPort}" Style="{StaticResource FormFieldLabelStyle}"/>
                                    <TextBox Style="{StaticResource FormTextBoxStyle}" Text="28016"/>

                                    <TextBlock Text="{DynamicResource Str_RconPass}" Style="{StaticResource FormFieldLabelStyle}"/>
                                    <PasswordBox Background="#0A0A0A" BorderBrush="#2A2A2A" Foreground="White" Padding="12,10" BorderThickness="1"/>
                                </StackPanel>

                                <!-- Column 2: Platform & Software -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="{DynamicResource Str_PlatformCore}" Foreground="{StaticResource PrimaryBrush}" FontSize="10" FontWeight="Bold"/>

                                    <TextBlock Text="{DynamicResource Str_UpdateBranch}" Style="{StaticResource FormFieldLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <RadioButton Content="STABLE" Foreground="White" IsChecked="True" FontSize="11" Margin="0,5,20,5"/>
                                        <RadioButton Content="STAGING" Foreground="#606060" FontSize="11" Margin="0,5,0,5"/>
                                    </StackPanel>

                                    <TextBlock Text="{DynamicResource Str_PluginCore}" Style="{StaticResource FormFieldLabelStyle}"/>
                                    <StackPanel>
                                        <RadioButton Margin="0,5">
                                            <RadioButton.Template>
                                                <ControlTemplate TargetType="RadioButton">
                                                    <Border Background="#101010" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10">
                                                        <TextBlock Text="CARBON" Foreground="#808080" FontSize="11" FontWeight="Bold"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="BorderBrush" Value="#FF4444"/>
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="False">
                                                            <Setter Property="BorderBrush" Value="#2A2A2A"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </RadioButton.Template>
                                        </RadioButton>
                                        <RadioButton Margin="0,5" IsChecked="True">
                                            <RadioButton.Template>
                                                <ControlTemplate TargetType="RadioButton">
                                                    <Border Background="#101010" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10">
                                                        <TextBlock Text="UOXIDE (RECOMMENDED)" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="BorderBrush" Value="#FF4444"/>
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="False">
                                                            <Setter Property="BorderBrush" Value="#2A2A2A"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </RadioButton.Template>
                                        </RadioButton>
                                        <RadioButton Margin="0,5">
                                            <RadioButton.Template>
                                                <ControlTemplate TargetType="RadioButton">
                                                    <Border Background="#101010" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="10">
                                                        <TextBlock Text="VANILLA" Foreground="#808080" FontSize="11" FontWeight="Bold"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="BorderBrush" Value="#FF4444"/>
                                                        </Trigger>
                                                        <Trigger Property="IsChecked" Value="False">
                                                            <Setter Property="BorderBrush" Value="#2A2A2A"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </RadioButton.Template>
                                        </RadioButton>
                                    </StackPanel>

                                    <TextBlock Text="{DynamicResource Str_InstallPath}" Style="{StaticResource FormFieldLabelStyle}"/>
                                    <Grid>
                                        <TextBox Style="{StaticResource FormTextBoxStyle}" Text="C:\RustServer" Padding="10,8" FontSize="11"/>
                                        <Button Content="📁" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="#505050" Margin="0,0,5,0" Width="30"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- STEP 2: PROGRESS VIEW -->
                    <StackPanel x:Name="Step2" Visibility="Collapsed" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource Str_ProgressTitle}" Style="{StaticResource InstallStepTitleStyle}" HorizontalAlignment="Center"/>
                        <TextBlock Text="45%" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,25"/>
                        
                        <Border Height="8" Width="500" Background="#101010" CornerRadius="4" BorderBrush="#2A2A2A" BorderThickness="1">
                            <Grid HorizontalAlignment="Left" Width="225">
                                <Border Background="{StaticResource PrimaryBrush}" CornerRadius="4">
                                    <Border.Effect><DropShadowEffect Color="#FF0000" BlurRadius="20" ShadowDepth="0" Opacity="0.9"/></Border.Effect>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
                            <Ellipse Width="8" Height="8" Fill="#FF4444" Margin="0,0,10,0">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Style.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.3" Duration="0:0:0.8" AutoReverse="True"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{DynamicResource Str_ProgressNote}" Foreground="#404040" FontSize="10" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!-- Footer -->
                <Border Grid.Row="2" Background="#0C0C0C" BorderThickness="0" CornerRadius="0,0,12,12">
                    <Grid Margin="15,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Button Style="{StaticResource HeaderButtonStyle}" Content="{DynamicResource Str_Cancel}" Width="120" Margin="0,0,15,0" BorderThickness="0" Click="Close_Click"/>
                            <Button x:Name="NextButton" Style="{StaticResource PremiumActionButtonStyle}" Content="{DynamicResource Str_InstallBtn}" Width="200" Height="40" Click="Next_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
